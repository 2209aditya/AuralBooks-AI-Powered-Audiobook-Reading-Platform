server:
  port: 8083

spring:
  application:
    name: marketplace-service

  datasource:
    url: jdbc:postgresql://postgres:5432/auralbooks
    username: admin
    password: password

  jpa:
    hibernate:
      ddl-auto: validate

  flyway:
    enabled: true

  kafka:
    bootstrap-servers: localhost:9092